apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src/'
        }
        // output.resourcesDir = file('../out/contest-core/bin/')
        // java.destinationDirectory.set(file('../out/contest-core/bin/'))
    }
}

dependencies {
    implementation fileTree('../shared/lib') { include '*.jar' }
}